// face-api-loader.tsからの再エクスポート
// 後方互換性のためにこのファイルを維持
export {
  ensureModelsLoaded,
  loadFaceApiModels,
  isModelsLoaded,
  extractFaceDescriptor,
  compareFaceDescriptors,
  createImageFromBase64,
  detectFaceFromVideo,
  compareFaces,
} from "./face-api-loader"
